///-----------------------------------------TODO List

- setup.sqf zu einer .hpp wandeln
- eventuel mehr prepared targets für die main mission. (set_maintarget.sqf z.b. artynest)

mehr jip compatibility: marker für rt, officer, shilkas, etc...
objekte wie player ai und farps werden nicht gelöscht wenn player disconnected
ace version prüfen (im editor anspielen wegen waffenkiste red und blue)


changelog since last relase:
Version number: 2.063.035

fixed: missing HALO roles in Red Chernarus version (#28390)
fixed: spectating mode didn't work for admins
fixed: added missing ammo and weapons (bug #28478)
fixed: stop rpt spaming of 'Unknown attribute height' and 'Unknown attribute width'
fixed: Speznas can make a HALO and deploy FARP's
fixed: game crash in A2 only versions (#28320)
fixed: variable with wrong network location (#28662)
fixed: Teamkill kick message occurs altough if player is admin (#28661)
fixed: MHQ(AHQ did not unlock itself if the admin disconnect (#28658)
fixed: player can jump in cargospace of the AHQ until it's locked (#28656)
fixed: injury system, only medics can revive (#30155)
fixed: wrong base class for engineers in Version Red, CO, Chernarus (#30123)
fixed: bonus points for air taxi aren't set in non lift choppers (#30950)

changed: prison disabled (no longer needed)
changed: rewriten vehicle respawn system (now tighter and smarter)

added: JIP compatibilty for known JIP's player rank (feature #28498)
added: now the supply points are JIP compatible (be stored after re-connect)


test








Notizen:

Für die automatische Wahl von Locations:
_arrayOfPlaces = nearestLocations [EVO_mapcenter,["namedergesuchtenLocation"],10000];



- ArmA2 CfgLocations:
http://community.bistudio.com/wiki/ArmA_2:_CfgLocationTypes



- Beispiel zum einschränken von Locations (Stückzahl):

/*
* we take heights as base.
* possible locations are heights that:
* - are at least 150m and at most 500m away from any road
* - flat and empty
* - at least 1800m away from friendly home base
*/
_all_possible_locations = [];
{
	_position = [position _x select 0, position _x select 1,0];

	_roads_close = _position nearRoads 150;
	_roads_far = _position nearRoads 500;
	_isflatempty = _position isflatempty [10,0,0.1,2,0,false,ObjNull];

	if(
		count _roads_far > 0
	     && count _roads_close == 0
	     && count _isflatempty > 0
	     && _position distance getmarkerpos format["respawn_%1",tfor_friendly_side] > 1800
	) then {_all_possible_locations = _all_possible_locations + [_x]};

} foreach _all_heights;